import { RecipeButton as RecipeButtonComponent } from './RecipeButton.component';
import * as css from './RecipeButton.css';

export const RecipeButton = Object.assign(RecipeButtonComponent, { css });
