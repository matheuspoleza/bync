import { style } from '@bync/style';

export const loaderStyles = style({
  borderRadius: 'inherit',
  zIndex: 1,
  position: 'absolute',
  height: 'inherit',
  width: 'inherit',
  top: 0,
  left: 0,
});

export const relativeContainerStyles = style({
  position: 'relative',
  width: 'inherit',
  height: 'inherit',
});
