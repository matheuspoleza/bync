import { style } from '@bync/style';

import { surfaceTokens } from '@/styles/theme/components';

export const imageLibraryStyle = style({
  display: 'block',
  borderRadius: surfaceTokens.borders.radius,
});
