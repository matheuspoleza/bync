import { style } from '@bync/style';

import { colors } from '@/styles/theme';

export const captionStyles = style({
  color: colors.neutralDark.neutralsDark50,
  fontWeight: 600,
});

export const turnsStyles = style({
  fontSize: '12px',
  lineHeight: '17px',
  margin: '0 4px 0',
  fontWeight: 600,
});

export const buttonStyles = style({
  marginTop: '10px',
});
