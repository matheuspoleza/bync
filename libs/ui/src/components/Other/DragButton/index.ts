import { DragButton as DragButtonComponent } from './DragButton.component';
import { DragButtonContainer as Container } from './DragButtonContainer/DragButtonContainer.component';

export const DragButton = Object.assign(DragButtonComponent, {
  Container,
});
