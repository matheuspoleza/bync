import { createThemeContract } from '@bync/style';

import { variableEntityTokens } from '@/styles/theme/components';

export const contract = createThemeContract(variableEntityTokens.color.entity.neutral);
