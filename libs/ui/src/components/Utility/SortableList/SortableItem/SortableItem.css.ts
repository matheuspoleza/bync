import { style } from '@bync/style';

export const hide = style({ visibility: 'hidden' });

export const dividerStyleBefore = style({ position: 'absolute', top: '-6px' });
export const dividerStyleAfter = style({ position: 'absolute', bottom: '-4px' });
