import { style } from '@bync/style';

export const containerStyle = style({
  width: '100%',
  position: 'relative',
});

export const contentStyle = style({
  position: 'absolute',
  top: 0,
  left: 0,
  width: '100%',
});
