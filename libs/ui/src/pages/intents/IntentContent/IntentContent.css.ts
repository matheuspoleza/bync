import { style } from '@bync/style';

export const intentProgressBar = style({
  width: '42px',
});

export const entityButton = style({
  position: 'relative',
});

export const footer = style({
  paddingLeft: 24,
});
