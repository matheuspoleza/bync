import { style } from '@bync/style';

import { colors } from '@/styles/theme';

export const dividerNegativeMargin = style({
  marginTop: -1,
});

export const textStyles = style({
  color: colors.neutralDark.neutralsDark100,
});
