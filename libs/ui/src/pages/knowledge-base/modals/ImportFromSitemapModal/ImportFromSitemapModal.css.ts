import { style } from '@bync/style';

import { colors } from '@/styles/theme';

export const submitButtonStyles = style({
  width: 101,
});

export const exampleLHSStyles = style({
  marginTop: 6,
  color: colors.neutralDark.neutralsDark200,
});

export const exampleLinkStyles = style({
  color: colors.neutralDark.neutralsDark50,
});

export const textAreaStyles = style({
  minHeight: '60px',
});
