import { style } from '@bync/style';

export const textareaStyles = style({
  maxHeight: 'calc(100vh - (32px * 2) - 56px - 60px - 16px - 24px - 20px - 24px)',
  overflowY: 'auto',
  overflowX: 'hidden',
});

export const importButtonStyles = style({
  width: 142,
});
