import { border, colors, shadows } from '../tokens';

export const color = {
  background: colors.white[100],
};

export const shadow = {
  default: shadows.surfaceShadows.z32Light,
};

export const borders = {
  radius: border.radius[10],
};
